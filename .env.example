# Copy to .env for local dev. In production these are set in the Render dashboard.
# (Shell stage needs none of these yet — they come in with auth + Sheets + features.)

# Supabase (your personal project)
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE=

# Google Sheets (the budget workbook is the source of truth)
GOOGLE_SERVICE_ACCOUNT_JSON=    # paste the service-account JSON (single line) or a path
SHEET_ID=1EQZiV2QqMz_9IfVQKUFM0LR24BJlXb9mGLcqAQmPflo

# AI + bank (reused from the pipeline)
ANTHROPIC_API_KEY=
PLAID_CLIENT_ID=
PLAID_SECRET=
