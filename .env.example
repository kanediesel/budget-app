# Copy to .env for local dev. In production these are set in the Render dashboard.

# --- WebAuthn / passkeys (MUST match the domain the app is served from) ---
# Local dev:
RP_ID=localhost
RP_ORIGIN=http://localhost:3000
# On Render, set:
#   RP_ID=budget-app-6hwn.onrender.com
#   RP_ORIGIN=https://budget-app-6hwn.onrender.com
RP_NAME=Budget

# Who may register a passkey (comma-separated; your household)
ALLOWED_EMAILS=brian.maucere@gmail.com

# Session signing (use a long random string in production)
SESSION_SECRET=change-me-to-a-long-random-string

# Supabase (your personal project)
SUPABASE_URL=https://xtqdyisgcnpmcfhtaakm.supabase.co
SUPABASE_SERVICE_KEY=     # the sb_secret_... key (server only)

# Google Sheets (source of truth)
GOOGLE_SERVICE_ACCOUNT_JSON=   # paste the full service-account JSON on one line
SHEET_ID=1EQZiV2QqMz_9IfVQKUFM0LR24BJlXb9mGLcqAQmPflo

# Claude (chat agent, receipt parse, transaction categorizer)
ANTHROPIC_API_KEY=

# Plaid production (server-side daily + on-demand account sync)
PLAID_CLIENT_ID=
PLAID_SECRET=
